// Named Exports (exporting.js)
export const sum = (a, b) => a + b;
export const multiply = (a, b) => a * b;

// Default Export (exporting.js)
export default function divide(a, b) {
  return a / b;
}
